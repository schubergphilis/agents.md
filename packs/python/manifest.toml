[pack]
name = "python"
description = "Mission-critical Python: uv, ruff, pyright, pytest — auditable, testable, safe for production"
version = "1.0.0"
authors = ["Schuberg Philis"]

[detect]
files = ["pyproject.toml", "setup.py", "requirements.txt"]

[targets]
agents_md = true
claude_md = true
commands = false
