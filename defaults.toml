[skills]
default = ["architecture-review", "deploy-checklist"]

[commands]
# Daily use — every engineer gets these
default = ["review", "challenge", "risk-check", "explain", "pre-deploy", "what-if-this-fails"]

# Contributor commands — installed but not highlighted in init output
contributor = ["new-pack", "new-skill"]
