# Auto-detection rules: file patterns → pack activation
# Only list packs that have content in packs/

[python]
files = ["pyproject.toml", "setup.py", "requirements.txt"]
pack = "python"

[github-actions]
files = [".github/workflows/*.yml", ".github/workflows/*.yaml"]
pack = "github-actions"
depends = ["supply-chain"]

# Uncomment when packs are created:
# [terraform]
# files = ["*.tf"]
# pack = "terraform"
#
# [docker]
# files = ["Dockerfile", "docker-compose.yml", "docker-compose.yaml"]
# pack = "docker"
#
# [kubernetes]
# files = ["k8s/*.yaml", "kubernetes/*.yaml", "helm/Chart.yaml"]
# pack = "kubernetes"
